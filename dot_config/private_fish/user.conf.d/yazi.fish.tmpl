{{ if and .configure.fish .configure.yazi .binaries.yazi -}}
function y
    set tmp (mktemp -t "yazi-cwd.XXXXXX")
    yazi $argv --cwd-file="$tmp"
    if read -z cwd < "$tmp"; and [ "$cwd" != "$PWD" ]; and test -d "$cwd"
        cd -- "$cwd"
    end
    rm -f -- "$tmp"
end

{{ end -}}

